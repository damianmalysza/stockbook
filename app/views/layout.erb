<!DOCTYPE html>
 <html class="no-js" lang="en"> <!--<![endif]-->
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge, chrome=1" />

    <title>Stockbook</title>

    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <link rel="stylesheet" href="/stylesheets/main.css" />
  </head>
  <body>
    <nav>
      <div><a href="/">Home</a></div>
      <div><a href="/watchlists">Watchlists</a></div>
      
      <% if logged_in? %>
      <div><a href="/users/<%=current_user.username%>">View Profile</a></div>
        <div><a href="/logout">Logout</a></div>
      <%else %>
      <div><a href="/signup">Signup</a></div>
      <div><a href="/login">Login</a></div>
      <% end %>
      
    </nav>
    <main>

        <%= yield %>
    </main>
      <footer class="branding">
        <small>&copy; 2021 <strong>Stockbook</strong></small>
      <% if logged_in? %>
        <div id="current-user">Logged in as: <em><%= current_user.username%></em></div>
      <% end %>
      </footer>

  </body>
</html>
